/**
 * Canonical project repository URL.
 *
 * Single source of truth for every in-app link that points at the project's
 * repository (top-bar GitHub icon, login-gate self-host hint, ...).  Keep it
 * here so a future repo migration only has to touch one place instead of
 * drifting per component.
 */
export const APP_REPO_URL = 'https://cnb.cool/siubeng/jp-lyrics-app';
